# Ping

Ping.

## Docker Setup

1. Build the dockers:

    ```bash
    make docker-build
    ```

2. Start the dockers:

    ```bash
    make docker-start
    ```

If you need to access the container, you may use:

```bash
make docker-enter
```
